const Logger = require('../util/Logger.js')('CMD', 'test');

const YouTube = require('../util/YouTube.js');

exports.run = async (Bot, msg, args) => {
	if (msg.author.id != '140210069596930048') return;

	let result = await YouTube.fetchInfo(args[0]);

	Logger.debug(result);
};
